//stop 停止播放

import web.layout.behavior.player;
var mpv = web.layout.behavior.player.mpv;

namespace web.layout.behavior.stop{
	onMouseClick = function( ltTarget,ltOwner,x,y,ltMouseParams ) {
		mpv.stop()
	}
}
