import util.metaProperty;
import aaz.libhpsocket.ssl.component.properties.httpSyncClient;

namespace aaz.libhpsocket.ssl.component;
var _dll = ..aaz.libhpsocket._dll;

class httpSyncClient{
	ctor(listener, pSocket){
		if(pSocket){
			this.pSocket = pSocket;
		}
		elseif(listener){
			this.pSocket = _dll.Create_HP_HttpsSyncClientP(listener);
			..table.gc(this, "destroy");
		}
		else {
			error("请指定参数@1 pSocket 或参数@2 pListener", 2);
		}
	};	
	destroy = function(){
		if(this.pSocket){
			_dll.Destroy_HP_HttpsSyncClient(this.pSocket);
			this.pSocket = null;
		}
	};	
	@_metaProperty;
}
httpSyncClient._metaProperty = ..util.metaProperty(..aaz.libhpsocket.ssl.component.properties.httpSyncClient);