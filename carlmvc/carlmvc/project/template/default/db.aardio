
//数据库操作层,sql语句请写在本层
import ${dbase};

var tableName = "${tableName}";		//表名
var tableKeyId = "${tableKeyId}";	//主键字段名
var tableKeyIdIsAuto = ${autoKeyId};//主键是否自增,由数据库自增设为true,由自己管理主键设为false

var database = ..${dbase}(tableName, tableKeyId, tableKeyIdIsAuto);
${db} = database;

namespace ${db}

string = ..string;
table = ..table;

//在这自定义数据库操作函数
