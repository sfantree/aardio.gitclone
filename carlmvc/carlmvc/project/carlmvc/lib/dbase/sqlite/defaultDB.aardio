
//数据库操作层,sql语句请写在本层
import dbase.sqlite;

var tableName = "default";
var tableKeyId = "defaultId";
var tableKeyIdIsAuto = false;//主键是否自增

var database = ..dbase.sqlite(tableName, tableKeyId, tableKeyIdIsAuto);
dbase.sqlite.defaultDB = database;

namespace dbase.sqlite.defaultDB

string = ..string;
table = ..table;

//在这自定义数据库操作函数
